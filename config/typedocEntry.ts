// The entry point for typedoc, which generates the methods reference in the
// documentation. It exports only the HtmlTree class, so the generated page
// documents the methods and nothing else.
export { default as HtmlTree } from "../src/index";
